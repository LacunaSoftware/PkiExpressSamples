doctype html
html
	head
		title PKI Express Samples
		link(rel='stylesheet', href='/stylesheets/bootstrap.css')
		link(rel='stylesheet', href='/stylesheets/bootstrap-theme.css')
		link(rel='stylesheet', href='/stylesheets/site.css')
body
	nav(class='navbar navbar-inverse navbar-fixed-top')
		div(class='container')
			div(class='navbar-header')
				button(type='button', class='navbar-toggle collapsed', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
					span(class='sr-only') Toggle navigation
					span(class='icon-bar')
					span(class='icon-bar')
					span(class='icon-bar')
				a(class='navbar-brand', href='/') PKI Express Samples
			div(id='navbar', class='collapse navbar-collapse')
				ul(class='nav navbar-nav')
					li(class='dropdown')
						a(href='#', class='dropdown-toggle', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false') PAdES signature
							span(class='caret')
						ul(class='dropdown-menu')
							li
								a(href='/pades-signature') With file already on server
								a(href='/upload?goto=pades-signature') With file uploaded by user
								a(href='/upload?goto=open-pades-signature') Open/validate existing signature
								a(href='/upload?goto=pades-signature-server-key') Using a server key
					li(class='dropdown')
						a(href='#', class='dropdown-toggle', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false') Batch signature
							span(class='caret')
						ul(class='dropdown-menu')
							li
								a(href='/batch-signature') Simple batch signature

	div(class='container')
		block content

	script(src='/javascripts/jquery-1.11.3.js')
	script(src='/javascripts/jquery.blockUI.js')
	script(src='/javascripts/bootstrap.js')

	script.
		var _webPkiLicense = "";

	block scripts